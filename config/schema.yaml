types:
    Departement:
        properties:
            code: { range: string }
            name: { range: string }
            de: { range: string }
    Commune:
        properties:
            code_insee: { range: string }
            name: { range: string }
            searchable: { range: string }
            departement: { range: Departement, cardinality: (*..0) }
            commonsCategory: { range: string }
            latitude: { range: float }
            longitude: { range: float }
    Church:
        properties:
            name: ~
            alternateName: ~
            description: ~
            address: { range: PostalAddress }
            commune: { range: Commune, cardinality: (*..0) }
            hasMap: ~
            telephone: ~
            faxNumber: ~
            wikidataId: { range: string }
            merimeeId: { range: string }
            egliseInfoId: { range: string }
            wikipediaId: { range: string }
            commonsId: { range: string }
            clochersId: { range: string }
            patrimoineReligieuxId: { range: string }
            url: { range: string }
            geo: { range: GeoCoordinates }
            photo: { range: string }
            logo: { range: string }
            isAccessibleForFree: ~
            publicAccess: ~
            maximumAttendeeCapacity: ~
            additionalType: ~
            event: { range: Event }
            review: { range: Review }
            openingHours: ~
            openingHoursSpecification: { range: OpeningHoursSpecification }
            specialOpeningHoursSpecification: { range: OpeningHoursSpecification }
    GeoCoordinates:
        properties:
            latitude: { range: Number }
            longitude: { range: Number }
    PostalAddress:
        properties:
            addressCountry: ~
            addressLocality: ~
            addressRegion: ~
            postalCode: ~
            streetAddress: ~
    Event:
        properties:
            name: ~
            description: ~
            subEvent: ~
            superEvent: ~
            location: { range: Church }
            isAccessibleForFree: ~
            inLanguage: { range: string }
            startDate: { range: DateTime }
            endDate: { range: DateTime }
            doorTime: ~
            duration: { range: number }
    OpeningHoursSpecification:
        properties:
            opens: ~
            closes: ~
            dayOfWeek: { range: DayOfWeek }
            validFrom: ~
            validThrough: ~
    DayOfWeek:
        properties:
            name: ~
    Review:
        properties:
            itemReviewed: { range: Church }
            reviewAspect: { range: string }
            reviewBody: ~
            reviewRating: { range: number }
